<%= turbo_stream.remove(@post) %>

<%# If all posts are deleted, render the empty state %>
<% unless Post.any? %>
    <%= turbo_stream.update dom_id(Post.new) do %>
        <%= render "empty" %>
    <% end %>
<% end %>