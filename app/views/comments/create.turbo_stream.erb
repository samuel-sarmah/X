<%= turbo_stream.append "#{dom_id(@post)}_comments" do %>
    <%= render partial: "comments/comment", locals: { comment: @comment } %>
<% end %>

<%= turbo_stream.update "#{dom_id(@post)}_comment_form" do %>
    <%= render "comments/form", post: @post %>
<% end %>
